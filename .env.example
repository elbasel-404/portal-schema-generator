# Portal API Generator Configuration
# Copy this file to .env and update with your actual values

# API Configuration
API_BASE_URL=https://your-portal-api.com
API_TOKEN=your-authentication-token-here
API_TIMEOUT=30000

# Default Employee ID (for testing and development)
# This is used in endpoint requests that require an employee_id
DEFAULT_EMPLOYEE_ID=305

# Generation Settings
OUTPUT_DIR=./schemas
CACHE_DIR=./json
LOG_LEVEL=info

# Development Settings
NODE_ENV=development

# Debug Settings (uncomment to enable debug logging)
# DEBUG=portal-api-gen:*
# DEBUG=portal-api-gen:http
# DEBUG=portal-api-gen:model

# Optional: Custom Headers
# CUSTOM_HEADER_NAME=value
# CUSTOM_HEADER_AUTH=Bearer token

# Optional: Proxy Settings (if required)
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=https://proxy.company.com:8080

# Optional: Request Settings
# REQUEST_RETRIES=3
# REQUEST_DELAY=1000

# Optional: File System Settings
# MAX_FILE_SIZE=10485760
# BACKUP_SCHEMAS=true