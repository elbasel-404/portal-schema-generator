# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   start:
#     runs-on: ubuntu-latest
#     steps:
#       - env:
#           API_KEY: ${{ secrets.API_KEY }}
#           API_ROOT_URL: ${{ secrets.API_ROOT_URL }}
#           API_KEY_HEADER_NAME: ${{ secrets.API_KEY_HEADER_NAME }}
#         uses: actions/checkout@v4
#       - uses: actions/setup-node@v2
#       - uses: oven-sh/setup-bun@v2
#       - run: |
#           echo "Before: corepack version => $(corepack --version || echo 'not installed')"
#           npm install -g corepack@latest
#           echo "After : corepack version => $(corepack --version)"
#           corepack enable
#           pnpm --version
#       - run: corepack enable
#       - run: corepack prepare pnpm@latest --activate
#       - run: |
#           touch .env
#           echo API_KEY=${{ secrets.API_KEY }} >> .env
#           echo API_ROOT_URL=${{ secrets.API_ROOT_URL }} >> .env
#           echo API_KEY_HEADER_NAME=${{ secrets.API_KEY_HEADER_NAME }} >> .env
#           cat .env
#       - run: pnpm install
#       - run: bun ./packages/scripts/generateAll.ts
#       - run: bun ./packages/scripts/validateAll.ts
